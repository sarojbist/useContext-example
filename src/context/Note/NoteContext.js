// Step 1: Import createContext from React
import { createContext } from "react";

// Step 2: Create a new context
const noteContext = createContext();

// Step 3: Export the created context
export default noteContext;
